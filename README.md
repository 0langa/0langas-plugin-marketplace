# 0langa Plugin Marketplace

Private plugin registry for Codex, Claude Code, and Kimi Code plugins.

## Registry

Plugin metadata lives in `plugins.json`. Per-plugin documentation lives under `plugins/<plugin-name>/`.

